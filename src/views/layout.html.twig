<! doctype html>

<html lang="fr">
    <head>
        <meta charset="utf-8" />
        <link rel="stylesheet" href="">
        <title>{% block title %}Test Silex{% endblock %}</title>
    </head>
    
    <body>
        <nav>
            <a href="{{ path('home') }}" title="Aller à l'accueil">Accueil</a>
            <a href="{{ path('listusers') }}" title="Voir la liste des users">Utilisateurs</a>            
        </nav>
        {% block content %}{% endblock %}
        <footer>Mon footer</footer>
    </body>
</html>